version: "3.9"

services:
  flaskapp:
    build: .
    container_name: flask-keycloak-demo
    ports:
      - "5000:5000"
    environment:
      KEYCLOAK_URL: "http://keycloak:8080"
      REALM: "demo-realm"
      CLIENT_ID: "flask-app"
    depends_on:
      - keycloak
    restart: always
