######################################################

docker run -d \
  -p 8080:8080 \
  -e KC_BOOTSTRAP_ADMIN_USERNAME=admin \
  -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin \
  -e PROXY_ADDRESS_FORWARDING=true \
  quay.io/keycloak/keycloak:26.4.6 start-dev


########################################################
